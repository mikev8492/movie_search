<h1>Results page</h1>

<% data['Search'].forEach(function(movie){ %>
  <ul>
    <li>
      <strong><%= movie['Title'] %></strong> - <%= movie['Year'] %>
    </li>
  </ul>
<% }); %>

<a href="/">
  <button type="button" name="again">Search Again</button>
</a>
